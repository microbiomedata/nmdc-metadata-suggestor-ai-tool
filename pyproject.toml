[project]
name = "nmdc-metadata-suggestor-ai-tool"
version = "1.2.1"
description = "NMDC Submission portal metadata suggestor tool, powered by AI"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
keywords = ["nmdc", "metadata", "doi", "llm", "bioinformatics"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3 :: Only",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Operating System :: OS Independent",
]
dependencies = [
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "google-auth>=2.0.0",
    "requests>=2.31.0",
    "curl-cffi>=0.13.0",
    "google-genai>=1.62.0",
    "linkml-runtime>=1.7.0",
    "nmdc-submission-schema>=11.0.0",
    "openai>=2.21.0",
    "claude-agent-sdk>=0.2.87",
    "pydantic-settings>=2.14.2",
    "python-multipart>=0.0.30",
    "starlette>=1.2.0",
    "langfuse>=4.14.0",
    "openinference-instrumentation-claude-agent-sdk>=0.1.7"
]

[project.urls]
Homepage = "https://github.com/microbiomedata/nmdc-metadata-suggestor-ai-tool"
Repository = "https://github.com/microbiomedata/nmdc-metadata-suggestor-ai-tool"
Issues = "https://github.com/microbiomedata/nmdc-metadata-suggestor-ai-tool/issues"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.23.0",
    "pytest-timeout>=2.3.0",
    "ruff>=0.3.0",
    "mypy>=1.8.0",
    "responses>=0.25.0",
    "types-requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "src/nmdc_metadata_suggestor_ai_tool",
]

[tool.hatch.build.targets.sdist]
include = [
    "src/nmdc_metadata_suggestor_ai_tool",
    "tests",
    "LICENSE",
    "README.md",
    "pyproject.toml",
]

[tool.ruff]
extend-exclude = [
    "scripts",
    "src/nmdc_metadata_suggestor_ai_tool/system_prompt.py"
]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = []

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.deptry.per_rule_ignores]
# nmdc-submission-schema ships schema YAML loaded at runtime via
# importlib.resources.files("nmdc_submission_schema.schema") in schema_context.py,
# so deptry's static import scan cannot see the usage (DEP002 false positive).
#
# other deps are here because of security vulnerabilities in their dependencies, 
# though they are not used directly by this project (DEP002 false positives).
DEP002 = ["nmdc-submission-schema", "pydantic-settings", "python-multipart", "starlette"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "integration: hits real external APIs (deselect with -m 'not integration')",
]
addopts = "-m 'not integration'"
